using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using TurnBasedPractice.GameClasses;

namespace TurnBasedPractice.GameClasses
{
    public class PlayerClass
    {
        /*
        private int id;
        private string name;
        private int constitutionBonus; //Used to derive HP | MaxHP = 5*Constitution
        private int magiBonus; //Used to derive mana | MaxMana = 5*Magi
        private int dexterityBonus; //Used to derive speed | Speed Bonus = (dexterity/2)Rounded
        private int strengthBonus; //Used to derive damage | Damage Bonus = (Strength/2)Rounded
        private int speedBonus; //Derived from dexterity (dex/2)
        private List<int> magiElementsCanUse = new List<int>();
        private Dictionary<Ability, int> abilitiesLearnedByLevel = new Dictionary<Ability, int>();
        */
    }
}
